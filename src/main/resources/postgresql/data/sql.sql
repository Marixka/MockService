CREATE TABLE responses (
                           id INTEGER PRIMARY KEY,
                           nameMethod VARCHAR(140),
                           abonent VARCHAR(140),
                           response VARCHAR(1400)
);

select * from responses;